<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Car - Cental</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header') %>

    <main class="admin-edit-car">
        <div class="form-container">
            <h1>Edit Car Details</h1>
            <form action="/admin/cars/edit/<%= car._id %>" method="POST" class="car-form" enctype="multipart/form-data">
                <div class="form-group">
                    <label for="brand">Brand:</label>
                    <input type="text" id="brand" name="brand" value="<%= car.brand %>" required>
                </div>

                <div class="form-group">
                    <label for="model">Model:</label>
                    <input type="text" id="model" name="model" value="<%= car.model %>" required>
                </div>

                <div class="form-group">
                    <label for="year">Year:</label>
                    <input type="number" id="year" name="year" value="<%= car.year %>" required>
                </div>

                <div class="form-group">
                    <label for="price">Price per Day ($):</label>
                    <input type="number" id="price" name="price" value="<%= car.price %>" required>
                </div>

                <div class="form-group">
                    <label for="carImage">Car Image:</label>
                    <div class="current-image">
                        <img src="<%= car.image %>" alt="Current car image" style="max-width: 200px; max-height: 150px; object-fit: cover; border-radius: 8px; margin-bottom: 10px;">
                        <p style="font-size: 0.9em; color: #666;">Current image</p>
                    </div>
                    <input type="file" id="carImage" name="carImage" accept="image/*">
                    <p style="font-size: 0.9em; color: #666; margin-top: 5px;">Leave empty to keep current image</p>
                </div>

                <div class="form-group">
                    <label for="description">Description:</label>
                    <textarea id="description" name="description" rows="4" required><%= car.description %></textarea>
                </div>

                <div class="form-group">
                    <label for="features">Features (comma-separated):</label>
                    <input type="text" id="features" name="features" value="<%= car.features.join(', ') %>" required>
                </div>

                <div class="form-group">
                    <label for="available">Availability:</label>
                    <select id="available" name="available">
                        <option value="true" <%= car.available ? 'selected' : '' %>>Available</option>
                        <option value="false" <%= !car.available ? 'selected' : '' %>>Unavailable</option>
                    </select>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn-primary">Update Car</button>
                    <a href="/admin/dashboard" class="btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html> 